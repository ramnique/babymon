services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    restart: unless-stopped
    ports:
      - '8080:8080' # put your reverse proxy (HTTPS) in front of this
    environment:
      TURN_SECRET: ${TURN_SECRET:?set TURN_SECRET in .env}
      TURN_URLS: turn:${PUBLIC_HOST:?set PUBLIC_HOST in .env}:3478?transport=udp,turn:${PUBLIC_HOST}:3478?transport=tcp
      STUN_URLS: ${STUN_URLS:-stun:stun.l.google.com:19302}

  coturn:
    image: coturn/coturn:4
    restart: unless-stopped
    # Host networking: TURN needs a range of UDP relay ports; mapping them
    # individually through Docker NAT is slow and error-prone.
    network_mode: host
    command: >-
      -n
      --listening-port=3478
      --min-port=49160
      --max-port=49200
      --use-auth-secret
      --static-auth-secret=${TURN_SECRET}
      --realm=${PUBLIC_HOST}
      --fingerprint
      --no-cli
      --no-multicast-peers
      --no-tlsv1
      --no-tlsv1_1
